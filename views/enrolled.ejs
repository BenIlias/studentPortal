<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        * {
            margin: 0;
        }

        .topnav {
            background-color: #333;
            overflow: hidden;
        }

        .enrolPara {
            background-color: #04AA6D;
            padding: 10px;
        }

        .invoiceref {
            color: crimson;
            font-size: 1em;
            font: bold;
        }

        /* Style the links inside the navigation bar */
        .topnav a {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
        }

        /* Change the color of links on hover */
        .topnav a:hover {
            background-color: #ddd;
            color: black;
        }

        /* Add a color to the active/current link */
        .topnav a.active {
            background-color: #04AA6D;
            color: white;
        }
    </style>
</head>

<body>
    <img src='Student-Portal-logo-v.png' height="50">
    <div class="topnav">
        <a class="active" href="home">Home</a>
        <a href="myenrollement">My Enrollements</a>
        <a href="#enrolled">Student Profile</a>
        <a href="#enrolled">Graduation</a>
        <a href="#enrolled">Sign Out</a>
    </div><br><br>


    <p class="enrolPara">You are enrolled in this course, Please log into the Payment Portal to pay the invoice
        reference <span class="invoiceref"></span>.</p>
    <h2 class="titleForm"></h2>
    <p class="courseDetails"></p>
    <p class="courseFees">Fees:</p>



    <script>

        
        const invoiceRef = document.querySelector('span.invoiceref')
        const titleForm = document.querySelector('h2.titleForm')
        const courseDetails = document.querySelector('.courseDetails')
        const courseFees = document.querySelector('.courseFees')

        invoiceRef.textContent = localStorage.getItem('invoiceRef')
        titleForm.textContent = localStorage.getItem('titleForm')
        courseDetails.textContent = localStorage.getItem('courseDetails')
        courseFees.textContent = localStorage.getItem('courseFees')
        






    </script>

</body>

</html>